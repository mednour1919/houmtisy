{% extends 'base.html.twig' %}

{% block title %}Nouvelle Zone de Collecte{% endblock %}

{% block body %}
<div class="container-fluid px-4">
    <div class="row">
        <div class="col-md-10 mx-auto mt-4">
            <!-- En-tête de page -->
            <div class="d-flex justify-content-between align-items-center mb-4 pb-2 border-bottom">
                <div>
                    <h1 class="h3 fw-bold mb-1">Nouvelle zone de collecte</h1>
                    <p class="text-muted mb-0">Ajouter une nouvelle zone à votre système</p>
                </div>
                <a href="{{ path('app_zone_de_collecte_crud_index') }}" class="btn btn-outline-secondary rounded-pill shadow-sm px-4">
                    Retour à la liste
                </a>
            </div>

            <!-- Form Card -->
            <div class="card shadow rounded-4 border-0 mb-4">
                <div class="card-header bg-transparent border-bottom-0 py-4">
                    <h5 class="mb-0 text-primary">
                        Formulaire de création
                    </h5>
                </div>
                <div class="card-body">
                    {{ include('zone_de_collecte_crud/_form.html.twig') }}
                </div>
            </div>

            <!-- Section d'aide -->
            <div class="alert alert-info shadow-sm rounded-4 mt-4 p-4">
                <div>
                    <h5 class="mb-2 fw-semibold">Conseils</h5>
                    <p class="mb-0">Remplissez tous les champs obligatoires marqués d'un astérisque (*). Vérifiez bien les informations avant de soumettre.</p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}