<form method="post" action="{{ path('app_zone_de_collecte_crud_delete', {'id': zone_de_collecte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zone_de_collecte.id) }}">
    <button class="btn">Delete</button>
</form>
