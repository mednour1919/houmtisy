{% extends 'base.html.twig' %}

{% block title %}Modifier Camion{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <!-- Sidebar chargée depuis base.html.twig -->
        <div class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
            <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                <h1 class="h2">Modification du camion</h1>
            </div>

            <div class="card shadow mb-4">
                <div class="card-body">
                    {{ include('camion_crud/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                </div>
            </div>

            <div class="d-flex justify-content-between mt-3">
                <div>
                    <a href="{{ path('app_camion_c_r_u_d_index') }}" class="btn btn-outline-secondary me-2">
                        Retour à la liste
                    </a>
                </div>
                <div>
                    {{ include('camion_crud/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}